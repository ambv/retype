[build-system]
requires = [
    "setuptools >= 41.0.0",
    "wheel >= 0.29.0",
    "setuptools-scm >= 2, < 4",
]
build-backend = 'setuptools.build_meta'

[tool.black]
line-length = 88
target-version = ['py36', 'py37', 'py38']
include = '\.pyi?$'
