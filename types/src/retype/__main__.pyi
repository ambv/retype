from typing import Sequence

def main(
    src: Sequence[str],
    pyi_dir: str,
    target_dir: str,
    incremental: bool,
    quiet: bool,
    replace_any: bool,
    hg: bool,
    traceback: bool,
) -> None: ...
